DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,testserver
DJANGO_SECRET_KEY=change-me-for-production
TRUST_X_FORWARDED_FOR=False
DJANGO_TRUST_X_FORWARDED_PROTO=False
DJANGO_CSRF_TRUSTED_ORIGINS=

# Production HTTPS settings. With DJANGO_DEBUG=False the core secure defaults
# are enabled automatically; uncomment only when you need explicit overrides.
# DJANGO_SECURE_SSL_REDIRECT=True
# DJANGO_SESSION_COOKIE_SECURE=True
# DJANGO_CSRF_COOKIE_SECURE=True
# DJANGO_SECURE_HSTS_SECONDS=31536000
# DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS=False
# DJANGO_SECURE_HSTS_PRELOAD=False

# Docker Compose and public domain.
SITE_DOMAIN=pnp1.ru
APP_PORT=8000
POSTGRES_HOST_PORT=5433
# PostgreSQL administrator password is used only by the database container.
POSTGRES_ADMIN_PASSWORD=change-me-separate-admin-password

# Bitrix24 incoming webhook URL with CRM access.
# Example: https://your-company.bitrix24.ru/rest/1/secret-code
BITRIX_WEBHOOK_URL=

# Public website-widget loader copied from Bitrix24. This is not a REST secret.
BITRIX_SITE_BUTTON_URL=https://bitrix.bus-sup.ru/upload/crm/site_button/loader_1_xry75s.js

# DeepSeek server-side AI assistant. Never expose the API key in templates or JS.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TIMEOUT_SECONDS=25

# SQLite is the local default.
SQLITE_NAME=db.sqlite3

# PostgreSQL option via separate variables.
# DB_ENGINE=postgresql
# DB_NAME=pnp1
# DB_USER=pnp1_app
# DB_PASSWORD=change-me
# DB_HOST=db
# DB_PORT=5432

# Shared cache inside Docker Compose. Leave unset for local development
# without Redis; Compose injects this value into the web container.
# REDIS_URL=redis://redis:6379/1

# PostgreSQL option via single URL.
# DATABASE_URL=postgresql://pnp1:change-me@127.0.0.1:5432/pnp1
